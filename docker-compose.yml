services:
  iris:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    command: --check-caps false --ISCAgent false
    ports:
      - "1972:1972"
      - "52773:52773"
      - "53773:53773"
